If-statements should always have braces
Do: 
```cpp
if (something) {
    // ...
}
```
Don't:
```cpp
if (something)
    // ...
```

`!` in if-statements should have a following space, like so:
```cpp
if (! variable)
```
